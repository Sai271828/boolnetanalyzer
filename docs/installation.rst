
Installation
=============

This package is available on PyPI and can be installed using pip:
    
    >>> pip install booleantools
    

To install the package from source, simply clone the repository using the following command:
    
    >>> git clone  git@github.com:LaboratoryForSystemsMedicine/boolean-networks.git
    
